# Scaling Considerations

- Separate frontend & backend deployments
- Use HTTP-only cookies for JWT in production
- Add refresh tokens
- Add pagination for tasks
- Introduce caching (Redis)
- Add CI/CD pipeline
- Rate limiting & monitoring
